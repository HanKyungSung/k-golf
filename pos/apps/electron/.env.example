############################################################
# Electron Dev Environment Variables (Example)
############################################################

# NOTE: ELECTRON_DEV is automatically set to 1 by 'npm run dev' script
# When ELECTRON_DEV=1, main process logs are forwarded to DevTools console
# In production builds, this is unset, so logs stay in terminal only

# Absolute path to the unpacked React DevTools extension folder
# This folder must contain a manifest.json (e.g. .../fmkadmapgofadopljbjfkapdkoienihi/\n# specific-version-number )
REACT_DEVTOOLS_PATH=/Users/you/Library/Application Support/Google/Chrome/Default/Extensions/fmkadmapgofadopljbjfkapdkoienihi/4.32.2_0

# Set any API base overrides if needed (fallback already in code)
# API_BASE_URL=http://localhost:8080

############################################################
# Usage:
# 1. Copy this file to .env in the same directory.
# 2. Edit REACT_DEVTOOLS_PATH to match your system.
# 3. Run: npm run dev:pos:electron (from repository root)
# Logs will show: [MAIN] REACT_DEVTOOLS_PATH= ... and extension load result.
############################################################# Example environment variables for POS Electron app
# Copy to .env and adjust as needed. Do NOT commit your real .env.

# Base URL of backend API (no trailing slash)
API_BASE_URL=https://k-golf.inviteyou.ca
# API_BASE_URL=http://localhost:8080

# Logging verbosity (silent|error|warn|info|debug)
LOG_LEVEL=info

# Interval (ms) between automatic sync attempts
SYNC_INTERVAL_MS=15000

# Optional fixed device identifier (otherwise app will generate & persist one)
# DEVICE_ID=YOUR-DEVICE-UUID

# Printer profile (future use): thermal58 | thermal80
# PRINTER_PROFILE=thermal58

# (Future) Toggle experimental features
# EXPERIMENTAL_PULL_SYNC=true

# Optional override for local SQLite file path (normally auto under data/)
# DB_PATH=./data/pos.sqlite
